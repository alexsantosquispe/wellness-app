export interface SessionType {
  id: string;
  title: string;
  category: string;
  duration: number;
  rating: number;
  image: string;
  description: string;
  instructor: string;
  date: string;
}
